filter:
    excluded_paths:
        - 'tests/*'
checks:
    php: true
coding_style:
    php:
        version: 7.0
        spaces:
            around_operators:
                concatenation: true
            other:
                after_type_cast: false
build: true